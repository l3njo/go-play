package card

// GetDeck returns an entire ordered deck as a slice
func GetDeck() []Card {
	// deck := []Card{}
	// for _, suit := range suits {
	// 	for _, face := range faces {
	// 		deck = append(deck, ard.GetCard(&Card{face: face, suit: suit}))
	// 	}
	// }
	// return deck
	return []Card{}
}

// Shuffle
// Get Random Deck
